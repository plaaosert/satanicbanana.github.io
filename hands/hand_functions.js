const hand_functions_leftclick_down = [
    // row 0
    [
        function(x, y) {},  // (0, 0)
        function(x, y) {},  // (1, 0)
        function(x, y) {},  // (2, 0)
        function(x, y) {},  // (3, 0)
        function(x, y) {},  // (4, 0)
        function(x, y) {},  // (5, 0)
        function(x, y) {},  // (6, 0)
        function(x, y) {},  // (7, 0)
    ],

    // row 1
    [
        function(x, y) {},  // (0, 1)
        function(x, y) {},  // (1, 1)
        function(x, y) {},  // (2, 1)
        function(x, y) {},  // (3, 1)
        function(x, y) {},  // (4, 1)
        function(x, y) {},  // (5, 1)
        function(x, y) {},  // (6, 1)
        function(x, y) {},  // (7, 1)
    ],

    // row 2
    [
        function(x, y) {},  // (0, 2)
        function(x, y) {},  // (1, 2)
        function(x, y) {},  // (2, 2)
        function(x, y) {},  // (3, 2)
        function(x, y) {},  // (4, 2)
        function(x, y) {},  // (5, 2)
        function(x, y) {},  // (6, 2)
        function(x, y) {},  // (7, 2)
    ],

    // row 3
    [
        function(x, y) {},  // (0, 3)
        function(x, y) {},  // (1, 3)
        function(x, y) {},  // (2, 3)
        function(x, y) {},  // (3, 3)
        function(x, y) {},  // (4, 3)
        function(x, y) {},  // (5, 3)
        function(x, y) {},  // (6, 3)
        function(x, y) {},  // (7, 3)
    ],

    // row 4
    [
        function(x, y) {},  // (0, 4)
        function(x, y) {},  // (1, 4)
        function(x, y) {},  // (2, 4)
        function(x, y) {},  // (3, 4)
        function(x, y) {},  // (4, 4)
        function(x, y) {},  // (5, 4)
        function(x, y) {},  // (6, 4)
        function(x, y) {},  // (7, 4)
    ],

    // row 5
    [
        function(x, y) {},  // (0, 5)
        function(x, y) {},  // (1, 5)
        function(x, y) {},  // (2, 5)
        function(x, y) {},  // (3, 5)
        function(x, y) {},  // (4, 5)
        function(x, y) {},  // (5, 5)
        function(x, y) {},  // (6, 5)
        function(x, y) {},  // (7, 5)
    ],

    // row 6
    [
        function(x, y) {},  // (0, 6)
        function(x, y) {},  // (1, 6)
        function(x, y) {},  // (2, 6)
        function(x, y) {},  // (3, 6)
        function(x, y) {},  // (4, 6)
        function(x, y) {},  // (5, 6)
        function(x, y) {},  // (6, 6)
        function(x, y) {},  // (7, 6)
    ],

    // row 7
    [
        function(x, y) {},  // (0, 7)
        function(x, y) {},  // (1, 7)
        function(x, y) {},  // (2, 7)
        function(x, y) {},  // (3, 7)
        function(x, y) {},  // (4, 7)
        function(x, y) {},  // (5, 7)
        function(x, y) {},  // (6, 7)
        function(x, y) {},  // (7, 7)
    ],
]

const hand_functions_rightclick_down = [
    // row 0
    [
        function(x, y) {},  // (0, 0)
        function(x, y) {},  // (1, 0)
        function(x, y) {},  // (2, 0)
        function(x, y) {},  // (3, 0)
        function(x, y) {},  // (4, 0)
        function(x, y) {},  // (5, 0)
        function(x, y) {},  // (6, 0)
        function(x, y) {},  // (7, 0)
    ],

    // row 1
    [
        function(x, y) {},  // (0, 1)
        function(x, y) {},  // (1, 1)
        function(x, y) {},  // (2, 1)
        function(x, y) {},  // (3, 1)
        function(x, y) {},  // (4, 1)
        function(x, y) {},  // (5, 1)
        function(x, y) {},  // (6, 1)
        function(x, y) {},  // (7, 1)
    ],

    // row 2
    [
        function(x, y) {},  // (0, 2)
        function(x, y) {},  // (1, 2)
        function(x, y) {},  // (2, 2)
        function(x, y) {},  // (3, 2)
        function(x, y) {},  // (4, 2)
        function(x, y) {},  // (5, 2)
        function(x, y) {},  // (6, 2)
        function(x, y) {},  // (7, 2)
    ],

    // row 3
    [
        function(x, y) {},  // (0, 3)
        function(x, y) {},  // (1, 3)
        function(x, y) {},  // (2, 3)
        function(x, y) {},  // (3, 3)
        function(x, y) {},  // (4, 3)
        function(x, y) {},  // (5, 3)
        function(x, y) {},  // (6, 3)
        function(x, y) {},  // (7, 3)
    ],

    // row 4
    [
        function(x, y) {},  // (0, 4)
        function(x, y) {},  // (1, 4)
        function(x, y) {},  // (2, 4)
        function(x, y) {},  // (3, 4)
        function(x, y) {},  // (4, 4)
        function(x, y) {},  // (5, 4)
        function(x, y) {},  // (6, 4)
        function(x, y) {},  // (7, 4)
    ],

    // row 5
    [
        function(x, y) {},  // (0, 5)
        function(x, y) {},  // (1, 5)
        function(x, y) {},  // (2, 5)
        function(x, y) {},  // (3, 5)
        function(x, y) {},  // (4, 5)
        function(x, y) {},  // (5, 5)
        function(x, y) {},  // (6, 5)
        function(x, y) {},  // (7, 5)
    ],

    // row 6
    [
        function(x, y) {},  // (0, 6)
        function(x, y) {},  // (1, 6)
        function(x, y) {},  // (2, 6)
        function(x, y) {},  // (3, 6)
        function(x, y) {},  // (4, 6)
        function(x, y) {},  // (5, 6)
        function(x, y) {},  // (6, 6)
        function(x, y) {},  // (7, 6)
    ],

    // row 7
    [
        function(x, y) {},  // (0, 7)
        function(x, y) {},  // (1, 7)
        function(x, y) {},  // (2, 7)
        function(x, y) {},  // (3, 7)
        function(x, y) {},  // (4, 7)
        function(x, y) {},  // (5, 7)
        function(x, y) {},  // (6, 7)
        function(x, y) {},  // (7, 7)
    ],
]

const hand_functions_leftclick_up = [
    // row 0
    [
        function(x, y) { set_hand(x, y, {pos: (get_hand(x, y).pos + 1) % 33}) },  // (0, 0)
        function(x, y) {},  // (1, 0)
        function(x, y) {},  // (2, 0)
        function(x, y) {},  // (3, 0)
        function(x, y) {},  // (4, 0)
        function(x, y) {},  // (5, 0)
        function(x, y) {},  // (6, 0)
        function(x, y) {},  // (7, 0)
    ],

    // row 1
    [
        function(x, y) {},  // (0, 1)
        function(x, y) {},  // (1, 1)
        function(x, y) {},  // (2, 1)
        function(x, y) {},  // (3, 1)
        function(x, y) {},  // (4, 1)
        function(x, y) {},  // (5, 1)
        function(x, y) {},  // (6, 1)
        function(x, y) {},  // (7, 1)
    ],

    // row 2
    [
        function(x, y) {},  // (0, 2)
        function(x, y) {},  // (1, 2)
        function(x, y) {},  // (2, 2)
        function(x, y) {},  // (3, 2)
        function(x, y) {},  // (4, 2)
        function(x, y) {},  // (5, 2)
        function(x, y) {},  // (6, 2)
        function(x, y) {},  // (7, 2)
    ],

    // row 3
    [
        function(x, y) {},  // (0, 3)
        function(x, y) {},  // (1, 3)
        function(x, y) {},  // (2, 3)
        function(x, y) {},  // (3, 3)
        function(x, y) {},  // (4, 3)
        function(x, y) {},  // (5, 3)
        function(x, y) {},  // (6, 3)
        function(x, y) {},  // (7, 3)
    ],

    // row 4
    [
        function(x, y) {},  // (0, 4)
        function(x, y) {},  // (1, 4)
        function(x, y) {},  // (2, 4)
        function(x, y) {},  // (3, 4)
        function(x, y) {},  // (4, 4)
        function(x, y) {},  // (5, 4)
        function(x, y) {},  // (6, 4)
        function(x, y) {},  // (7, 4)
    ],

    // row 5
    [
        function(x, y) {},  // (0, 5)
        function(x, y) {},  // (1, 5)
        function(x, y) {},  // (2, 5)
        function(x, y) {},  // (3, 5)
        function(x, y) {},  // (4, 5)
        function(x, y) {},  // (5, 5)
        function(x, y) {},  // (6, 5)
        function(x, y) {},  // (7, 5)
    ],

    // row 6
    [
        function(x, y) {},  // (0, 6)
        function(x, y) {},  // (1, 6)
        function(x, y) {},  // (2, 6)
        function(x, y) {},  // (3, 6)
        function(x, y) {},  // (4, 6)
        function(x, y) {},  // (5, 6)
        function(x, y) {},  // (6, 6)
        function(x, y) {},  // (7, 6)
    ],

    // row 7
    [
        function(x, y) {},  // (0, 7)
        function(x, y) {},  // (1, 7)
        function(x, y) {},  // (2, 7)
        function(x, y) {},  // (3, 7)
        function(x, y) {},  // (4, 7)
        function(x, y) {},  // (5, 7)
        function(x, y) {},  // (6, 7)
        function(x, y) {},  // (7, 7)
    ],
]

const hand_functions_rightclick_up = [
    // row 0
    [
        function(x, y) { set_hand(x, y, {pos: (get_hand(x, y).pos - 1) % 33}) },  // (0, 0)
        function(x, y) {},  // (1, 0)
        function(x, y) {},  // (2, 0)
        function(x, y) {},  // (3, 0)
        function(x, y) {},  // (4, 0)
        function(x, y) {},  // (5, 0)
        function(x, y) {},  // (6, 0)
        function(x, y) {},  // (7, 0)
    ],

    // row 1
    [
        function(x, y) {},  // (0, 1)
        function(x, y) {},  // (1, 1)
        function(x, y) {},  // (2, 1)
        function(x, y) {},  // (3, 1)
        function(x, y) {},  // (4, 1)
        function(x, y) {},  // (5, 1)
        function(x, y) {},  // (6, 1)
        function(x, y) {},  // (7, 1)
    ],

    // row 2
    [
        function(x, y) {},  // (0, 2)
        function(x, y) {},  // (1, 2)
        function(x, y) {},  // (2, 2)
        function(x, y) {},  // (3, 2)
        function(x, y) {},  // (4, 2)
        function(x, y) {},  // (5, 2)
        function(x, y) {},  // (6, 2)
        function(x, y) {},  // (7, 2)
    ],

    // row 3
    [
        function(x, y) {},  // (0, 3)
        function(x, y) {},  // (1, 3)
        function(x, y) {},  // (2, 3)
        function(x, y) {},  // (3, 3)
        function(x, y) {},  // (4, 3)
        function(x, y) {},  // (5, 3)
        function(x, y) {},  // (6, 3)
        function(x, y) {},  // (7, 3)
    ],

    // row 4
    [
        function(x, y) {},  // (0, 4)
        function(x, y) {},  // (1, 4)
        function(x, y) {},  // (2, 4)
        function(x, y) {},  // (3, 4)
        function(x, y) {},  // (4, 4)
        function(x, y) {},  // (5, 4)
        function(x, y) {},  // (6, 4)
        function(x, y) {},  // (7, 4)
    ],

    // row 5
    [
        function(x, y) {},  // (0, 5)
        function(x, y) {},  // (1, 5)
        function(x, y) {},  // (2, 5)
        function(x, y) {},  // (3, 5)
        function(x, y) {},  // (4, 5)
        function(x, y) {},  // (5, 5)
        function(x, y) {},  // (6, 5)
        function(x, y) {},  // (7, 5)
    ],

    // row 6
    [
        function(x, y) {},  // (0, 6)
        function(x, y) {},  // (1, 6)
        function(x, y) {},  // (2, 6)
        function(x, y) {},  // (3, 6)
        function(x, y) {},  // (4, 6)
        function(x, y) {},  // (5, 6)
        function(x, y) {},  // (6, 6)
        function(x, y) {},  // (7, 6)
    ],

    // row 7
    [
        function(x, y) {},  // (0, 7)
        function(x, y) {},  // (1, 7)
        function(x, y) {},  // (2, 7)
        function(x, y) {},  // (3, 7)
        function(x, y) {},  // (4, 7)
        function(x, y) {},  // (5, 7)
        function(x, y) {},  // (6, 7)
        function(x, y) {},  // (7, 7)
    ],
]
